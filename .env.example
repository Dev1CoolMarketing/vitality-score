# Rayhawk API host. You can use either the host root or the /v1 base path.
NEXT_PUBLIC_RAYHAWK_API_URL=http://localhost:8080

# Optional T-Shots web host used for desktop sign-in links.
NEXT_PUBLIC_TSHOTS_WEB_URL=https://t-shots.com
